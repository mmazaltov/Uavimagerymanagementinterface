# Workflow для разработки и деплоя

## Архитектура окружений

Проект использует два окружения для безопасного тестирования и развертывания изменений:

### 1. **Staging (Локальное тестирование)** 🧪
- **Ветка**: `claude/staging-BhLK7`
- **URL**: http://localhost:3000 (локальный dev сервер)
- **Назначение**: Тестирование новых функций перед релизом в production
- **Запуск**: `npm run dev`
- **Преимущества**:
  - Мгновенные изменения (Hot Module Replacement)
  - Полноценная отладка в браузере
  - Нет ожидания деплоя

### 2. **Production (Продакшн)** 🌐
- **Ветка**: `claude/cv-agro-code-BhLK7`
- **URL**: https://e3a.ru
- **Назначение**: Стабильная рабочая версия приложения для конечных пользователей
- **Деплой**: Автоматический при push в `claude/cv-agro-code-BhLK7`
- **Workflow**: `.github/workflows/deploy.yml`

---

## Процесс разработки

### Шаг 1: Локальная разработка и тестирование

1. Переключитесь на staging ветку:
   ```bash
   git checkout claude/staging-BhLK7
   ```

2. Убедитесь что зависимости установлены:
   ```bash
   npm install
   ```

3. Запустите локальный dev сервер:
   ```bash
   npm run dev
   ```

   Сайт откроется на http://localhost:3000

4. Вносите изменения в коде - они автоматически обновляются в браузере

5. Тестируйте функциональность:
   - ✅ Все функции работают корректно
   - ✅ Нет визуальных багов
   - ✅ Карты загружаются
   - ✅ Изображения отображаются
   - ✅ Навигация работает

6. Коммитите изменения:
   ```bash
   git add .
   git commit -m "Add new functionality"
   git push origin claude/staging-BhLK7
   ```

### Шаг 2: Релиз в Production

**Только после успешного тестирования локально!**

1. Переключитесь на production ветку:
   ```bash
   git checkout claude/cv-agro-code-BhLK7
   ```

2. Смержите изменения из staging:
   ```bash
   git merge claude/staging-BhLK7
   ```

3. Запушьте в production:
   ```bash
   git push origin claude/cv-agro-code-BhLK7
   ```

4. GitHub Actions автоматически задеплоит изменения на e3a.ru

5. Подождите 2-3 минуты

6. Проверьте что всё работает на production:
   ```
   https://e3a.ru
   ```

7. Очистите кэш браузера: **Ctrl+Shift+R** (или **Cmd+Shift+R**)

---

## Быстрые команды

### Локальная разработка

```bash
# Запуск dev сервера (staging)
git checkout claude/staging-BhLK7
npm run dev

# Сборка проекта для тестирования
npm run build

# Предпросмотр production билда локально
npm run preview
```

### Релиз в production

```bash
# Смержить staging в production
git checkout claude/cv-agro-code-BhLK7
git merge claude/staging-BhLK7
git push
```

### Работа с ветками

```bash
# Переключиться на staging для новых изменений
git checkout claude/staging-BhLK7

# Переключиться на production для релиза
git checkout claude/cv-agro-code-BhLK7

# Посмотреть текущую ветку
git branch

# Посмотреть статус изменений
git status
```

### Проверка деплоя

```bash
# Посмотреть последние коммиты
git log --oneline -5

# Посмотреть статус в GitHub Actions
# Откройте: https://github.com/mmazaltov/Uavimagerymanagementinterface/actions
```

---

## Важные правила ⚠️

### ✅ МОЖНО:
- Вносить любые изменения в `claude/staging-BhLK7` ветку
- Тестировать экспериментальные функции локально
- Делать несколько коммитов в staging перед релизом в production
- Откатывать изменения в staging если что-то сломалось

### ❌ НЕЛЬЗЯ:
- Пушить напрямую в `claude/cv-agro-code-BhLK7` без тестирования локально
- Удалять ветку `claude/staging-BhLK7` или `claude/cv-agro-code-BhLK7`
- Изменять файлы конфигурации деплоя без понимания последствий
- Коммитить секреты или credentials

---

## Структура проекта

```
.
├── .github/
│   └── workflows/
│       └── deploy.yml              # Production деплой (claude/cv-agro-code-BhLK7 → e3a.ru)
├── public/
│   ├── CNAME                       # Домен e3a.ru (только в production ветке)
│   └── .nojekyll                   # Отключает Jekyll в GitHub Pages
├── src/                            # Исходный код приложения
├── vite.config.ts                  # Конфигурация Vite (base: '/' в обеих ветках)
└── package.json                    # Зависимости проекта
```

---

## Почему staging на localhost, а не на отдельном URL?

**Ограничение GitHub Pages:** Один репозиторий = один сайт.

Невозможно одновременно деплоить:
- Production на e3a.ru
- Staging на github.io/staging

Локальная разработка имеет преимущества:
- ⚡ Мгновенные изменения (Hot Module Replacement)
- 🐛 Полноценная отладка в DevTools
- 🚀 Нет ожидания деплоя (2-3 минуты)
- 💰 Не расходует GitHub Actions минуты

---

## Troubleshooting

### Локальный dev сервер не запускается

1. Проверьте что Node.js установлен:
   ```bash
   node --version
   # Должна быть версия 18+
   ```

2. Переустановите зависимости:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. Попробуйте другой порт:
   ```bash
   npm run dev -- --port 3001
   ```

### Production (e3a.ru) не обновляется

1. Проверьте что изменения запушены:
   ```bash
   git status
   git log --oneline -3
   ```

2. Проверьте статус деплоя:
   - Откройте https://github.com/mmazaltov/Uavimagerymanagementinterface/actions
   - Найдите последний workflow "Deploy Production to GitHub Pages"
   - Проверьте нет ли ошибок

3. Очистите кэш браузера:
   - Нажмите **Ctrl+Shift+R** (или **Cmd+Shift+R** на Mac)

4. Проверьте что CNAME файл существует:
   ```bash
   git checkout claude/cv-agro-code-BhLK7
   cat public/CNAME
   # Должно показывать: e3a.ru
   ```

### Забыли в какой ветке находитесь

```bash
git branch
# Звездочка * показывает текущую ветку
```

### Нужно откатить изменения

**В staging (безопасно):**
```bash
git checkout claude/staging-BhLK7
git reset --hard HEAD~1  # Откатить последний коммит
git push origin claude/staging-BhLK7 --force
```

**В production (осторожно!):**
```bash
git checkout claude/cv-agro-code-BhLK7
git revert HEAD          # Создать revert коммит
git push origin claude/cv-agro-code-BhLK7
```

### Конфликты при merge

Если при мерже staging в production возникли конфликты:

```bash
git checkout claude/cv-agro-code-BhLK7
git merge claude/staging-BhLK7

# Если конфликты:
# 1. Откройте файлы с конфликтами в редакторе
# 2. Разрешите конфликты вручную
# 3. Добавьте исправленные файлы:
git add .
git commit -m "Merge staging into production"
git push
```

---

## Типичный рабочий день

### Утро - начало новой фичи:
```bash
git checkout claude/staging-BhLK7
git pull origin claude/staging-BhLK7
npm run dev
# Разрабатываете функционал, тестируете в браузере
```

### День - коммиты по мере работы:
```bash
git add .
git commit -m "Add feature X"
git push origin claude/staging-BhLK7
# Продолжаете разработку
```

### Вечер - релиз в production:
```bash
# Закрываете dev сервер (Ctrl+C)
git checkout claude/cv-agro-code-BhLK7
git pull origin claude/cv-agro-code-BhLK7
git merge claude/staging-BhLK7
git push origin claude/cv-agro-code-BhLK7

# Через 2-3 минуты проверяете на https://e3a.ru
```

---

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте GitHub Actions логи
2. Проверьте консоль браузера (F12 → Console)
3. Проверьте этот файл WORKFLOW.md
4. Создайте issue в репозитории
