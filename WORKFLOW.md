# Workflow для разработки и деплоя

## Архитектура окружений

Проект использует два окружения для безопасного тестирования и развертывания изменений:

### 1. **Production (Продакшн)** 🌐
- **Ветка**: `main`
- **URL**: https://e3a.ru
- **Назначение**: Стабильная рабочая версия приложения для конечных пользователей
- **Деплой**: Автоматический при push в `main`
- **Workflow**: `.github/workflows/deploy.yml`

### 2. **Staging (Тестовое окружение)** 🧪
- **Ветка**: `claude/staging-BhLK7`
- **URL**: https://mmazaltov.github.io/Uavimagerymanagementinterface-staging/
- **Назначение**: Тестирование новых функций перед релизом в production
- **Деплой**: Автоматический при push в `claude/staging-BhLK7`
- **Workflow**: `.github/workflows/deploy-staging.yml`

---

## Процесс разработки

### Шаг 1: Разработка новых функций

1. Переключитесь на ветку `claude/staging-BhLK7`:
   ```bash
   git checkout claude/staging-BhLK7
   ```

2. Создайте feature ветку от staging (опционально):
   ```bash
   git checkout -b feature/new-functionality
   ```

3. Вносите изменения в коде

4. Коммитите изменения:
   ```bash
   git add .
   git commit -m "Add new functionality"
   ```

### Шаг 2: Тестирование в Staging

1. Запушьте изменения в ветку `claude/staging-BhLK7`:
   ```bash
   git push origin claude/staging-BhLK7
   ```

   Или если работаете в feature ветке:
   ```bash
   git checkout claude/staging-BhLK7
   git merge feature/new-functionality
   git push origin claude/staging-BhLK7
   ```

2. GitHub Actions автоматически задеплоит изменения на staging URL

3. Подождите 2-3 минуты пока деплой завершится

4. Откройте staging URL и протестируйте изменения:
   ```
   https://mmazaltov.github.io/Uavimagerymanagementinterface-staging/
   ```

5. Проверьте:
   - ✅ Все функции работают корректно
   - ✅ Нет визуальных багов
   - ✅ Карты загружаются
   - ✅ Изображения отображаются
   - ✅ Навигация работает

### Шаг 3: Релиз в Production

**Только после успешного тестирования в staging!**

1. Переключитесь на ветку `main`:
   ```bash
   git checkout main
   ```

2. Смержите изменения из `claude/staging-BhLK7`:
   ```bash
   git merge claude/staging-BhLK7
   ```

3. Запушьте в `main`:
   ```bash
   git push origin main
   ```

4. GitHub Actions автоматически задеплоит изменения на production (e3a.ru)

5. Подождите 2-3 минуты

6. Проверьте что всё работает на production:
   ```
   https://e3a.ru
   ```

---

## Быстрые команды

### Локальная разработка

```bash
# Запуск dev сервера
npm run dev

# Сборка проекта
npm run build

# Предпросмотр production билда
npm run preview
```

### Работа с ветками

```bash
# Переключиться на staging для новых изменений
git checkout claude/staging-BhLK7

# Переключиться на main для релиза
git checkout main

# Посмотреть текущую ветку
git branch

# Посмотреть статус изменений
git status
```

### Проверка деплоя

```bash
# Посмотреть последние коммиты
git log --oneline -5

# Посмотреть статус в GitHub Actions
# Откройте: https://github.com/mmazaltov/Uavimagerymanagementinterface/actions
```

---

## Важные правила ⚠️

### ✅ МОЖНО:
- Вносить любые изменения в `claude/staging-BhLK7` ветку
- Тестировать экспериментальные функции в staging
- Делать несколько коммитов перед релизом в main
- Откатывать изменения в staging если что-то сломалось

### ❌ НЕЛЬЗЯ:
- Пушить напрямую в `main` без тестирования в staging
- Удалять ветку `claude/staging-BhLK7` или `main`
- Изменять файлы конфигурации деплоя без понимания последствий
- Коммитить секреты или credentials

---

## Структура проекта

```
.
├── .github/
│   └── workflows/
│       ├── deploy.yml           # Production деплой (main → e3a.ru)
│       └── deploy-staging.yml   # Staging деплой (staging → github.io)
├── public/
│   ├── CNAME                    # Только в main ветке (домен e3a.ru)
│   ├── .nojekyll                # Отключает Jekyll в GitHub Pages
│   └── weeds/                   # Изображения сорняков (если используются)
├── src/                         # Исходный код приложения
├── vite.config.ts               # Конфигурация Vite
│                                # staging: base: '/Uavimagerymanagementinterface-staging/'
│                                # main: base: '/'
└── package.json                 # Зависимости проекта
```

---

## Troubleshooting

### Staging не обновляется после push

1. Проверьте что вы в правильной ветке:
   ```bash
   git branch
   # Должно показывать: * staging
   ```

2. Проверьте статус деплоя:
   - Откройте https://github.com/mmazaltov/Uavimagerymanagementinterface/actions
   - Найдите workflow "Deploy Staging to GitHub Pages"
   - Проверьте нет ли ошибок

3. Очистите кэш браузера:
   - Нажмите **Ctrl+Shift+R** (или **Cmd+Shift+R** на Mac)

### Production (e3a.ru) не работает

1. Проверьте что CNAME файл существует в main ветке:
   ```bash
   git checkout main
   cat public/CNAME
   # Должно показывать: e3a.ru
   ```

2. Проверьте base URL в vite.config.ts:
   ```bash
   grep "base:" vite.config.ts
   # Должно быть: base: '/',
   ```

3. Проверьте статус деплоя в Actions

4. Проверьте DNS настройки домена

### Нужно откатить изменения

**В staging (безопасно):**
```bash
git checkout claude/staging-BhLK7
git reset --hard HEAD~1  # Откатить последний коммит
git push origin claude/staging-BhLK7 --force
```

**В production (осторожно!):**
```bash
git checkout main
git revert HEAD          # Создать revert коммит
git push origin main
```

---

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте GitHub Actions логи
2. Проверьте консоль браузера (F12 → Console)
3. Создайте issue в репозитории
