# 🔧 Инструкции по настройке базы данных

## ❌ Проблема
```
Error: Could not find the table 'public.kv_store_faa4b35a' in the schema cache
```

Таблица `kv_store_faa4b35a` не существует в вашей базе данных Supabase. Эта таблица необходима для хранения метаданных снимков БПЛА.

---

## ✅ Решение

### Шаг 1: Откройте SQL Editor

Перейдите по ссылке: **https://supabase.com/dashboard/project/wuhndvrjxzfkdwdjtzih/editor**

### Шаг 2: Создайте новый запрос

В Supabase Dashboard:
1. В левом меню выберите **SQL Editor** (значок с кодом)
2. Нажмите кнопку **New Query**

### Шаг 3: Выполните SQL

Скопируйте и вставьте следующий SQL код:

```sql
CREATE TABLE IF NOT EXISTS kv_store_faa4b35a (
  key TEXT NOT NULL PRIMARY KEY,
  value JSONB NOT NULL
);
```

Нажмите кнопку **Run** (или используйте Ctrl+Enter)

### Шаг 4: Проверьте результат

Вы должны увидеть сообщение: **"Success. No rows returned"**

Это означает, что таблица успешно со��дана!

### Шаг 5: Перезагрузите приложение

Обновите страницу приложения (F5). Красное уведомление об ошибке должно исчезнуть.

---

## 🧪 Проверка работоспособности

После создания таблицы протестируйте функционал:

1. Откройте Developer Console (нажмите F12)
2. Перейдите на вкладку **"Инспекции"** в приложении
3. Нажмите кнопку **"Добавить снимок"**
4. Заполните форму:
   - Название снимка *
   - Дата и время *
   - Координаты (опционально)
   - Описание (опционально)
   - Выберите файл изображения
5. Нажмите **"Сохранить снимок"**

В консоли браузера вы должны увидеть логи:
```
✅ Starting snapshot upload...
✅ Upload successful
✅ Snapshot created successfully
```

Новый снимок должен появиться в секции "Загруженные снимки" на странице инспекции.

---

## 📋 Техническая информация

| Параметр | Значение |
|----------|----------|
| **Имя таблицы** | `kv_store_faa4b35a` |
| **Схема** | `public` |
| **Тип** | Key-Value хранилище |
| **Формат значений** | JSONB |
| **Использование** | Хранение метаданных снимков БПЛА |
| **Storage bucket** | `make-7e600dc3-uav-images` (создается автоматически) |

### Структура таблицы

- `key` (TEXT, PRIMARY KEY) - уникальный идентификатор записи
  - Формат для снимков: `snapshot:{inspectionId}:{timestamp}`
- `value` (JSONB) - JSON объект с данными снимка:
  ```json
  {
    "id": "snapshot:1:1234567890",
    "inspectionId": "1",
    "name": "Название снимка",
    "date": "2024-10-21T10:30:00",
    "coordinates": "55.7558° N, 37.6173° E",
    "description": "Описание",
    "imagePath": "1/1234567890.jpg",
    "createdAt": "2024-10-21T10:30:00.000Z"
  }
  ```

---

## 🔍 Альтернативная проверка существующих таблиц

Если вы хотите проверить, какие таблицы уже существуют:

1. Откройте **Table Editor** в Supabase Dashboard
2. Посмотрите список всех таблиц в схеме `public`
3. Если вы видите другую таблицу с похожим именем (например, `kv_store_7e600dc3`), это может указывать на несоответствие версий

---

## 🆘 Устранение неполадок

### Проблема: Таблица создана, но ошибка остается

**Решение:**
1. Убедитесь, что таблица создана в схеме `public`
2. Проверьте имя таблицы (должно быть точно `kv_store_faa4b35a`)
3. Очистите кэш браузера (Ctrl+Shift+Delete)
4. Перезапустите Edge Function в Supabase Dashboard

### Проблема: SQL возвращает ошибку прав доступа

**Решение:**
1. Убедитесь, что вы используете правильный проект в Supabase
2. Проверьте, что у вашего пользователя есть права на создание таблиц
3. Попробуйте выполнить SQL от имени владельца проекта

### Проблема: Снимки не отображаются после загрузки

**Решение:**
1. Откройте консоль браузера и проверьте логи
2. Проверьте, что Storage bucket `make-7e600dc3-uav-images` создан
3. Проверьте серверные логи в Supabase > Edge Functions > Logs

---

## 📞 Дополнительная помощь

Если проблема не решена:

1. **Проверьте логи сервера**: Supabase Dashboard > Edge Functions > server > Logs
2. **Проверьте логи браузера**: Откройте Developer Console (F12) > Console
3. **Проверьте настройки Supabase**: Dashboard > Settings > API

При запуске сервера в логах должно быть сообщение:
```
✓ KV table exists and is ready
✓ Bucket make-7e600dc3-uav-images already exists
```

Если вместо этого вы видите:
```
❌ Table kv_store_faa4b35a does not exist!
```

То таблица не была создана - повторите шаги выше.
